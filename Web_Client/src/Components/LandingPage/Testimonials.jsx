/*
import React from "react";
import { testimonials } from "../../constants";

const Testimonials = () => {
  return (
    <div className="mt-20 tracking-wide">
      <h2 className="text-3xl sm:text-4xl lg:text-5xl text-center my-10 lg:my-20 font-bold">
        What people are saying
      </h2>
      <div className="flex flex-wrap justify-center">
        {testimonials.map((testimonial, index) => (
          <div key={index} className="w-full sm:w-1/2 lg:w-1/3 p-4 text-center">
            <div className="bg-neutral rounded-md p-6 text-md border border-neutral-800 font-thin">
              <div className="flex mb-8 items-center  ">
                <img
                  className="w-12 h-12 mr-5 rounded-full border border-neutral-300"
                  src={testimonial.image}
                  alt={testimonial.user}
                />
                <div className="ml-[-3px]">
                  <h6 className="font-bold ml-[-40px]">{testimonial.user}</h6>
                  <span className="test-sm font-normal italic text-neutral-600 mb-3">
                    {testimonial.company}
                  </span>

                  <div className="text-yellow-400 flex  w-42 h-30 gap-1 ">
                    {testimonial.Rating}
                  </div>
                </div>
              </div>
              <p>{testimonial.text}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};

export default Testimonials;

*/
